export const obtainProduct = async () => {
    return await fetch('')
        .then(() => {return true})
        .catch()
        .finally()
}
export const sendOffer = async () => {
    return await fetch('')
        .then(() => {return true})
        .catch()
        .finally()
}